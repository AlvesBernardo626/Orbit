# Estado do projeto

Implementação inicial em andamento. Estrutura: npm workspaces; Electron/React; API Express; MongoDB; contratos Zod compartilhados.

Etapas: estrutura → autenticação → perfis → amigos → mensagens → grupos → eventos → voz → tela → revisão de segurança → testes → configuração de deploy.

A mídia inicial usa P2P conforme o pedido atual, com transporte isolado para migração futura para SFU. Não há credenciais reais nem deploy realizado.
